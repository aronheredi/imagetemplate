import type { EditProps } from "@/types/canvas";



export const CircleEdit = ({ selectedObject, update }: EditProps) => {
    return (
        <div className='flex flex-col gap-3'>

        </div>);
}
