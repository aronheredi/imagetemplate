# Docker Compose Environment Variables
# These are used by docker-compose.yml for PostgreSQL and MinIO containers

# PostgreSQL Configuration
DB_USER=postgres
DB_PASSWORD=password
DB_NAME=postgres

# MinIO Configuration
MINIO_USER=minioadmin
MINIO_PASSWORD=minioadmin
